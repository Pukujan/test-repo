# Checkpoint 0004 — Run 2 local-002 (potion-base-32M)

## Status

Controlled iteration complete. Same frozen train/calibration/test IDs as Run 1; only Potion base model changed to `minishlab/potion-base-32M` with temperature re-fit on calibration only.

## What was executed

1. Created `scripts/run2_pipeline.py` loading frozen fixtures from `inputs/fixtures/` (verified digests match run-001).
2. Kept LocalClosedSetScoreProvider LLM-substitute path (no hosted LLM).
3. Trained Potion/Model2Vec `minishlab/potion-base-32M` on train only; temperature-scaled on calibration.
4. Evaluated both systems on the same frozen test IDs; wrote metrics, comparison, error analysis.
5. Full-run observability reconciliation (OTel file exporter; Langfuse still not configured). Smoke not separately required (harness unchanged) but smoke subset still executed by the shared pipeline.
6. Updated `checks.json` evidence for run-002; wrote this checkpoint; refreshed `conclusion.md` / `state.json`.

## Result vs Run 1

| Metric | Run 1 Potion 8M | Run 2 Potion 32M |
|--------|-----------------|------------------|
| Accuracy | 0.99 | 0.99 |
| Macro-F1 | 0.9900 | 0.9900 |
| Log loss | 0.0545 | 0.0522 |
| Brier | 0.0198 | 0.0215 |
| ECE | 0.0098 | 0.0126 |
| Potion-only error | syn-0176 | syn-0176 |

LLM-substitute remained accuracy 1.0. Larger embedding did not fix the single hard error on this synthetic fixture; calibration log-loss improved slightly.

## Substitutes / limitations (honest)

- No hosted LLM / Langfuse — documented substitutes unchanged.
- Synthetic fixture ceiling: one persistent Potion error (`syn-0176`) across 8M and 32M.

## Evidence root

`runs/run-20260918-local-002/`
