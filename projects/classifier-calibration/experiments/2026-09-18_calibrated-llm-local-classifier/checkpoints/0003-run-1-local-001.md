# Checkpoint 0003 — Run 1 local-001

## Status

Run 1 acceptance target met. Experiment status set to `completed` with a concrete next hypothesis for controlled iteration.

## What was executed

1. Created public-safe synthetic multi-class fixture (`inputs/fixtures/synthetic_topics_v1.jsonl`, 400 rows, labels: sports/technology/politics/health).
2. Froze stratified disjoint train / calibration / test IDs (seed `20260918`) in `split_manifest.json`.
3. Configured observability content mode `hashes_only` (OBSERVABILITY.md `hash`).
4. Created `runs/run-20260918-local-001/` with the PLAN.md layout plus OTel file traces.
5. Smoke subset (12 test IDs): stable IDs, OTel JSONL traces, structured events, privacy mode, reconciled counts.
6. Closed-set scoring on calibration + test via **LocalClosedSetScoreProvider** (documented LLM substitute emitting per-class logits).
7. Fit temperature scaling on calibration only; recorded pre/post metrics.
8. Trained Potion/Model2Vec (`minishlab/potion-base-8M`) on train only; temperature-scaled on calibration.
9. Evaluated both on the same frozen test IDs; wrote metrics, confusion, `comparison.json`, `error_analysis.md`.
10. Full-run observability reconciliation + `observability.json` (Langfuse not configured; no secrets invented).
11. Updated `checks.json` with PASS + evidence for all Run 1 checks.

## Substitutes / gaps (honest)

- **LLM path:** no hosted LLM API keys and no Ollama/vLLM server → `local-closed-set-score-provider/tfidf-logreg-v1` (logits for temperature scaling). Documented in `llm_config.json` / `run.json`.
- **Langfuse:** not configured → OTel `FileSpanExporter` → `otel_traces.jsonl` as inspectable traces.
- **Potion:** real Model2Vec training path used (not TF-IDF fallback).

## Next hypothesis

Potion/Model2Vec errs more than the score provider. Next controlled change: try potion-base-32M (larger static embedding) with the same train/calibration/test IDs and re-fit temperature scaling on calibration only.

## Evidence root

`runs/run-20260918-local-001/`
