# Shared-chat completeness and durable roundtrip

This is Experiment #1 for the lab.

It uses the real public ChatGPT share from the product-design conversation that led to `Pukujan/fossil-core#247`.

The experiment is deliberately broader than the bug alone. First it must reproduce and characterize acquisition completeness. If that succeeds, it continues through durable FOSSIL ingestion, semantic query/lineage, projection destruction/rebuild, and an inventory of what is actually portable today.

Do not start from assumptions about the consumer product. Record what current FOSSIL actually does.

Resume from `HANDOFF.md`.
